/*    4.	Looking at the list of DOS commands, test        6 more commands executed within a C program.*/#include <stdio.h>int main(){    printf("*** 1. IPCONFIG: Displays IP Information");    system("ipconfig");    printf("\n*** 2. ATTRIB: Displays attributes of a file");    system("attrib 5-4.c");    printf("\n*** 3. VER: Displays the version of Windows");    system("ver");    printf("\n*** 4. CIPHER: Displays the encryption state of the directory and any files it contains");    system("cipher 5-4folder");    printf("\n*** 5. ECHO: Displays a message");    system("echo This is a Message");    printf("\n*** 6. PING: Verifies IP-level connectivity to another TCP/IP computer");    system("ping suu.edu");}