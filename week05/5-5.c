/*    5.	Add code to the testppm program that automatically        opens your image in gimp after it is created.*/#include <stdio.h>#include <math.h>#include <stdlib.h>int main(){    int r,g,b,x,y,w=640,h=640;    FILE *f1=fopen("5-5.ppm","w");    fprintf(f1,"P3\n%d %d\n255\n",w,h);    for (y=0 ; y<h ; ++y){        for (x=0 ; x<w ; ++x){            r = (255.0 * y)/h;            g = cos(0.001*(pow(x,1.3) * pow(y,1.3))) * 255.0;            b = (255.0 * x)/w;            fprintf(f1, "%03d %03d %03d ", r, g, b);        }        fprintf(f1, "\n");    }    fclose(f1);    system("open -a /Applications/GIMP.app 5-5.ppm");    return 0;}