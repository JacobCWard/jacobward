/*    3.	Create two new different and interesting images.*/#include <stdio.h>#include <math.h>int main(){    int r,g,b,x,y,w=640,h=640;    FILE *f1=fopen("5-3a.ppm","w");    fprintf(f1,"P3\n%d %d\n255\n",w,h);    for (y=0 ; y<h ; ++y){            for (x=0 ; x<w ; ++x){                    r = sin(0.005*(pow(x,1.1) * pow(y,1.1))) * 255.0;                    g = (255.0 * y)/h;                    b = (255.0 * x)/w;                    fprintf(f1, "%03d %03d %03d ", r, g, b);                }                fprintf(f1, "\n");            }    fclose(f1);    FILE *f2=fopen("5-3b.ppm","w");    fprintf(f2,"P3\n%d %d\n255\n",w,h);    for (y=0 ; y<h ; ++y){        for (x=0 ; x<w ; ++x){            r = 128;            g = fmod(0.0001*(pow(x-w/2,3) + pow(y-w/2,3)),255.0);            b = fmod(0.0001*(pow(3*w/2-x,3) + pow(3*h/2-y,3)),255.0);            fprintf(f2, "%03d %03d %03d ", r, g, b);        }        fprintf(f2, "\n");    }    fclose(f2);    return 0;}