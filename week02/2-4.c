/*    4.	Write a program that, given positive integer n from the console,        passes the array [1,...,n] to a function which computes the square of        each element and returns the result to the caller.*/#include <stdio.h>int n;int * squareIt(int a[n]){    int i;    for(i=0;i<n;i++) a[i]=a[i]*a[i];    int * aptr = a;    return aptr;}int main(){    int i;    printf("Enter a value for n: ");    scanf("%d",&n);    if(n<1){        printf("n must be positive\n");        return 0;    }    int a[n], b[n];    for(i=0;i<n;i++){        printf("Enter a[%d]: ",i);        scanf("%d",&a[i]);        b[i]=a[i];    }    int * aptr=squareIt(a);    for(i=0;i<n;i++) printf("a[%d] = %d \ta[%d]² = %d\n",i,b[i],i,a[i]);    return 0;}