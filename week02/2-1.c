/*    1.	Write a program that, given positive integer n from the console, computes the        first n Fibonacci numbers (you probably don’t want a recursive algorithm).NOTE: n must be ≤ 47. 48th fibonacci number is > INT_MAX.*/#include <stdio.h>int main(){    int n, a=0, b=1, count=2, current;    printf("Please enter a value for n: ");    scanf("%d",&n);    if(n>0) printf("%d",a);    if(n>1) printf(", %d",b);    while (count<n){        current=a+b;        a=b;        b=current;        printf(", %d",current);        count++;    }    printf("\n");    return 0;}