/*1.	Write a function which takes two integer arrays and returns “true”if the two arrays have exactly the same elements.*/#include <stdio.h>int arrayCompare(int x[], int y[], int size);int main(){    int i,al,bl;    printf("Enter the size for array a: ");    scanf("%d",&al);    int a[al];    printf("Enter the values for array a:\n");    for(i=0;i<al;i++){        scanf("%d",&a[i]);    }    printf("Enter the size for array b: ");    scanf("%d",&bl);    int b[bl];    printf("Enter the values for array b:\n");    for(i=0;i<bl;i++){        scanf("%d",&b[i]);    }    printf("Contents of a:\t");    for(i=0;i<al;i++){        printf("%d\t",a[i]);    }    printf("\nContents of b:\t");    for(i=0;i<bl;i++){        printf("%d\t",b[i]);    }    if(arrayCompare(a,b,(sizeof(a)/sizeof(*a)))){        printf("\ntrue\n");    }    else{        printf("\nfalse\n");    }}int arrayCompare(int x[], int y[], int size){    int i;    for(i=0;i<size;i++){        if(x[i] != y[i]){            return 0;        }    }    return 1;}